/**
 * Una variable de cada tipo
Para este ejercicio, debemos declarar 4 variables de tipo let con los nombres: verdadero, nada, texto y numero. A cada una de ellas, tendremos que asignarle el tipo de dato que sugiera su nombre, según corresponda.
Dame una pista
Para la variable a la cual le asignes el nombre verdadero queremos que le agregues un dato del tipo booleano; a la que llames texto queremos que le asignes un string; a la que declares como número que le agregues un dato numérico; por último: ¿se te ocurre que le asignarías a la variable llamada nada? (Te recomendamos como buena práctica que utilices la palabra reservada let para declarar tus variables) CUIDADO: Respetá en los nombres de las variables mayúsculas y minúsculas, y cuidado con las tildes.
 */
let verdadero = true;
let nada = null;
let texto = "texto"
let numero = 10;
